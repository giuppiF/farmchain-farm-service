stages:
 - build
#=========
# Build
#=========
build:
  stage: build
  image: backstreetacademy/docker-aws
  only:
    refs:
      - master
  services: 
    - docker:dind
  script: 
    - chmod +x ./build.sh
    - ./build.sh